@model placeCase

<div class="modal fade" id="add-model" tabindex="-1" role="dialog" aria-labelledby="add-model-label" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="add-model-label">Tạo mới tài khoản</h5>
                <button type="button" localize-content class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form asp-action="Create" enctype="multipart/form-data">
                    <input asp-for="Id" name="check" type="hidden" />
                    <input name="IsValid" type="hidden" value="@ViewData.ModelState.IsValid.ToString()" />
                    <div class="form-group">
                        <label class="control-label" localize-content>Tên địa điểm sai</label>
                        <input asp-for="nameCase" class="form-control" required />
                        <span asp-validation-for="nameCase" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label class="control-label" localize-content>Mã địa điểm chuẩn</label>
                        <input asp-for="placeCode" class="form-control" required />
                        <span asp-validation-for="placeCode" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label class="control-label" localize-content>Mã địa điểm cấp trên</label>
                        <input asp-for="placeFatherCode" class="form-control" required />
                        <span asp-validation-for="placeFatherCode" class="text-danger"></span>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" localize-content class="btn btn-secondary" data-dismiss="modal">Thoát</button>
                <button type="button" localize-content class="btn btn-primary" data-save="modal">Tạo</button>
            </div>
        </div>
    </div>
</div>


@await Html.PartialAsync("_ValidationScriptsPartial")