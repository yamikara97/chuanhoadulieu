@model places

<div class="modal fade" id="add-model" tabindex="-1" role="dialog" aria-labelledby="add-model-label" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="add-model-label">Tạo mới tài khoản</h5>
                <button type="button" localize-content class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form asp-action="Create" enctype="multipart/form-data">
                    <input asp-for="Id" name="check" type="hidden" />
                    <input name="IsValid" type="hidden" value="@ViewData.ModelState.IsValid.ToString()" />
                    <div class="form-group">
                        <label class="control-label" localize-content>Tên địa điểm</label>
                        <input asp-for="NameOutput" class="form-control" required />
                        <span asp-validation-for="NameOutput" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label class="control-label" localize-content>Mã</label>
                        <input asp-for="Code" class="form-control" required />
                        <span asp-validation-for="Code" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label class="control-label" localize-content>Mã đơn vị cha</label>
                        <input asp-for="FatherId" class="form-control" required />
                        <span asp-validation-for="FatherId" class="text-danger"></span>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" localize-content class="btn btn-secondary" data-dismiss="modal">Thoát</button>
                <button type="button" localize-content class="btn btn-primary" data-save="modal">Tạo</button>
            </div>
        </div>
    </div>
</div>


@await Html.PartialAsync("_ValidationScriptsPartial")